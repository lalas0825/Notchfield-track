/**
 * Sprint 69 — Notifications screen route.
 * Thin wrapper around <NotificationsScreen/>; entry point is the bell
 * icon in the Home header (NotificationBell). Hidden from the tab bar
 * via `href: null` in (tabs)/_layout.tsx.
 */
export { default } from '@/features/notifications/components/NotificationsScreen';
