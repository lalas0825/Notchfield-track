/**
 * Sprint 70 — Today screen route.
 * Thin wrapper around <TodayScreen/>; entry point is the checklist icon
 * in the Home header (TodayHeaderIcon). Hidden from the tab bar via
 * `href: null` in (tabs)/_layout.tsx.
 */
export { default } from '@/features/todos/components/TodayScreen';
